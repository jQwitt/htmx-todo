mixin todo(title, dueDate)
    div.flex.flex-row.justify-between.items-center.my-2.border.border-slate-500.rounded.p-2.bg-slate-100
      div
        p.text-xl.font-bold(isChecked ? ) #{title}
        p.text-sml.font-bold Due: #{dueDate}
      input.w-5.h-5.m-4(type="checkbox")
